# Procfile - Tells the deployment platform how to run your app
# ============================================================
# 
# This single line tells Railway/Render:
# "Run uvicorn to serve the FastAPI app in main.py"
#
# --host 0.0.0.0 : Accept connections from any IP
# --port $PORT   : Use the port assigned by the platform

web: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}
